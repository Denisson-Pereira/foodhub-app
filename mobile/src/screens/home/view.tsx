import { ScrollView, Text } from "react-native";

export const HomeView = () => {
    return (
        <ScrollView
            showsVerticalScrollIndicator={false}
        >
            <Text>home</Text>
        </ScrollView>
    );
}